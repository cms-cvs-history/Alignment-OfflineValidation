process TEST = 
{ 

  service = MessageLogger {
        untracked vstring destinations = {"detailedInfo", "cout"}
  }

  #Ideal geometry
  include "Geometry/CMSCommonData/data/cmsIdealGeometryXML.cfi"
  include "Geometry/TrackerNumberingBuilder/data/trackerNumberingGeometry.cfi"


  source = EmptySource {}
  untracked PSet maxEvents = { untracked int32 input = 0 }

  module comparator = TrackerGeometryCompare{

	untracked string inputFileType = "root"

	untracked string inputROOTFile1 = "idealtracker16x.root"
	untracked string inputROOTFile2 = "idealtracker17x.root"
	untracked string treeName = "alignTree"


	untracked string inputDBType = "tracker"
	untracked string outputFile = "testCompare.root"

##	untracked vstring levels = {"PixelEndcap","PixelHalfBarrel","TID","HalfBarrel","Endcap","DetUnit"}
	untracked vstring levels = {"DetUnit"}

  }

  path p = { comparator }









}
