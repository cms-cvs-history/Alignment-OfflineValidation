process TEST = 
{ 

  service = MessageLogger {
        untracked vstring destinations = {"detailedInfo", "cout"}
  }

  source = EmptySource {}
  untracked PSet maxEvents = { untracked int32 input = 0 }


  module convert = Tracker_OldtoNewConverter{
        untracked string conversionType = "OldtoNew"
	untracked string treeName = "alignTree"
        untracked string inputFile = "../data/HIPalignmentsF.root"
        untracked string outputFile = "../data/HIPalignmentsF_converted.root"
        untracked string textFile = "../data/PositionDetIDMap.txt"
  }


  path p = { convert }









}
