process TEST = 
{ 

  service = MessageLogger {
        untracked vstring destinations = {"detailedInfo", "cout"}
  }

  #Ideal geometry
  include "Geometry/CMSCommonData/data/cmsIdealGeometryXML.cfi"
  include "Geometry/TrackerNumberingBuilder/data/trackerNumberingGeometry.cfi"

  #---------------------------------------------------------------------
  #Here, one would do a proper configuration of POOLESSource for the DB object
  #Example below
  #--------------------------------------------------------------------- 
#  include "CondCore/DBCommon/data/CondDBSetup.cfi"
#  es_source = PoolDBESSource
#  {
#        using CondDBSetup
#        string connect = "sqlite_file:MisalignedGeometry100pb.db"
#        string timetype = "runnumber"
#        untracked string catalog = "file:condDBMisaligned.xml"
#        VPSet toGet = { {string record = "TrackerAlignmentRcd"  string tag = "Tracker100pbScenario150" },
#                        {string record = "TrackerAlignmentErrorRcd" string tag = "Tracker100pbScenarioErrors150" }}
#  }


  source = EmptySource {}
  untracked PSet maxEvents = { untracked int32 input = 0 }

  module dump = TrackerGeometryIntoNtuples{
	untracked string outputFile = "idealtracker.root"
	untracked string outputTreename = "alignTree"
  }

  path p = { dump }









}
